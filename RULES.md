# 项目规则与约定

更新日期：2026-09-09。本文件记录已确认的内容约定与项目操作要求；当前总规划见 `PROJECT_PLAN.md`，历史 `000-plan/` 文件仅作追溯，不覆盖最新约定。用户后续明确要求优先于本文件。

- 读者为高年级本科生。每讲约 90 分钟，包含理论、案例或代码、练习；控制每讲核心目标数量。
- Stata 为主要计量软件，`nbstata` Notebook 展示执行结果；Python 用于数据处理与必要分析。
- 仓库身份固定为 `lianxhcn/FinEco`，本地目录固定为 `D:/github_lianxh/FinEco`，默认分支为 `main`，网站主标题为“金融计量经济学”。
- 正式目录固定为 `lectures/`、`notebooks/`、`data/`、`exercises/`、`resources/`，网站输出为 `docs/`。章节文件名使用稳定英文主题名，不绑定授课序号；编号由 Quarto 目录决定。引用标签采用主题前缀。
- 不涉及代码演示、包含较多公式或交叉引用的正文使用 `.qmd`；包含 Stata 或 Python 代码及其执行结果的章节使用 `.ipynb`。理论与代码并重时拆为 `.qmd` 理论篇和 `.ipynb` 实验篇，作为同一主题的两部分维护。纯说明和任务清单可以使用 `.md`。
- 一个 Notebook 固定一个主内核；同章 Stata 与 Python 演示优先分开存放，通过明确的数据文件衔接，避免隐含的跨内核状态。
- 教师课前从头运行并保存 Notebook 输出，课堂只展示预先跑通的代码和结果，不依赖现场执行。禁止手工编造 Notebook 输出或用渲染成功替代复现检查。
- 数据获取与模型分析分离，保留带抓取日期的数据快照、参数、口径和来源；网站渲染及课堂展示不依赖实时 API。冻结快照用于复现，不自动代表符合历史可得信息要求。
- 发布路线固定为“本地执行并保存输出 → 本地渲染 → 检查 → 推送静态网页”。代码变更后必须重新执行对应 Notebook，再更新页面，不能展示过期输出。
- `00#-` 开头的文件夹保留在本地，不推送。教材、受限数据、教师答案和工作草稿不进入网页输出。
- 允许读取和整合用户授权的来源课程；不得删除、改写或移动来源项目的原文件。引用时记录来源文件、读取深度和改写范围。
- 中文采用朴实的学术讲义风格；中英文间加空格，括号使用半角，数学上下标加花括号，simulation 译为“模拟”。
- 每个数值结果须有可运行代码与输出支持。模拟数据明确标注，不伪装为真实金融数据。
- AI 可辅助规划、编码与资料初筛；识别策略、样本构造、推断和结论由人核验，作业保留关键过程记录。
- 环境路径使用用户配置的环境变量；存在项目 `.venv` 时优先使用。不得因旧材料中的版本信息覆盖现有全局环境。
- 后续生成或插入图片时遵守全局 PicGo 规则：本地保存至 `figs/raw/`，唯一文件名，通过本机 PicGo 上传并维护链接清单，不读取图床配置或密钥。
- 首讲与网站已上线，当前进入后续章节写作。发布沿用已有授权，不重复询问；并行写作时由一个整合窗口统一构建和推送，章节窗口不各自发布共享网站。发布前完成构建与公开范围检查，不读取任何凭据。
- 删除事件研究法；CAPM 与投资组合分章；ARMA 与 GARCH/VaR 分为两讲。Lasso 基本原理与交叉验证必讲，DDML 单列重点章节并优先整合 ci-policy 材料。动态面板必须独立成章，课堂安排由教师视课时决定。
- 第一讲采用“10 只股票组合”和金融政策因果推断两个案例。环境配置放独立页面，以 Agent 工作流程为主线、传统分步操作为补充，课堂只用约 10 分钟讲要点。
- 书稿章节数不等于 90 分钟授课次数；全书可以包含弹性授课章节。考核沿用课程大纲：平时考勤 10%、课后作业 30%、期末考试 60%。许可证待确认，不套用参考仓库的许可。


## 教学写作：已经确认

- 面向需要自学的本科生，提供平缓的理解阶梯。小节先解释要解决什么、为什么要做，再展开方法、步骤或例子；相关小节讲完后自然收束，不机械地每节都写总结。
- 并列情形采用平行结构。例如数据清洗先作总体说明，再用复权、缺失值、样本选择三个列表项分别解释问题、处理与经济含义。操作步骤用有序列表，概念分类用无序列表，不能把整个讲义压成缺少铺垫的提纲。
- 每个公式首次出现时，先给经济直觉，再解释所有新符号：下标、变量、参数、取值范围、单位、数据频率、估计或界定方法。优化问题还须解释目标函数、约束和可行性；年化、超额收益和风险指标须明确口径。
- 数据划分、模型选择和检验前先说明目的与依据。区分训练、验证、测试；说明如何避免未来信息泄漏。结果解释指向实际表格、图形和统计量，同时说明不能得出的结论。
- 开篇重点框统一称“本讲要点”。重要概念、操作建议和易错边界可用 callout；不将普通段落全部装框。
- 参考当前已发布的 lectures/intro.qmd 的讲解深度、列表与公式说明，不继承旧草稿的提纲式写法。

## 导航、作业与文献：已经确认

- Part 与章节使用简短标题，尽量避免侧栏换行。正式 Part 通过 styles.css 加粗，不手写章节序号。前置功能页可用表情符号，环境配置为“🛠️ 环境配置”。
- 首页、课程资料、课件使用、课程安排之后是作业总览；环境配置在正式讲义之前。未完成章节不加入公开目录。
- homework.qmd 只维护共同要求和各次作业入口；任务各用 exercises/hw-01.qmd、hw-02.qmd 等独立页面。页面首尾提供返回作业总览、课程主页和提交入口。
- 作业编号和目录使用 hw-01 等无空格名称。每学期每位学生一个个人仓库，与教师课件副本分开放置；教师副本更新不能覆盖学生作业。
- 首次课可在线阅读，之后克隆一次、课前更新。Desktop 与 Agent 获取路线二选一，推荐 Agent；强制对齐须限定教师副本并先备份，禁止作用于个人作业仓库。
- hw-01 暂不强制 Pages；原则上从 hw-02 过渡，启用日期与截止时间由教师通知，不能自行编造。统一提交地址为 https://send2me.cn/dXoHZt1Q/TZOX0ZWijy7xAg 。
- 公共书目 resources/references.qmd 收录跨章教材和经典文献，提供各章书目入口。每章末尾保留该章完整引用及相关数据、软件来源；不同时手工维护第二份完整总表。
- 课程资料 resources.qmd 提供学习资源与在线书入口，不能与公共书目混为同一用途。章末参考文献采用 myAPA，按作者排序，核验 DOI、PDF 和 Google 链接；不能凭空补全。
- 当前封面使用 fineco-fig01-cover-20260909-152237.png；Logo 使用 fineco-fig02-icon-40-20260909-150949.png。沿用已上传链接，不重新生成，不给封面添加学期或课程编号。封面之外的课程基本信息可按学期更新。

## 并行窗口的操作要求

这些要求用于避免覆盖共享文件；章节分配、是否采用 worktree 仍须按 PROJECT_PLAN.md 中的待讨论事项确定。

1. 开始前读 AGENTS.md、RULES.md、PROJECT_PLAN.md、_quarto.yml 和本章相关文件，并检查 git status。用户指定本章后，记录负责的文件范围；不要自行认领其他章。
2. 章节窗口只编辑本章 lectures/<topic>.qmd、notebooks/<topic>/、data/<topic>/、tools/<topic>/ 等明确范围。共享数据快照只读；衍生数据写入本章目录，记录来源版本与构造步骤。
3. _quarto.yml、styles.css、首页、作业总览、公共书目、全局依赖、公共运行器和 docs/ 由整合窗口维护。章节窗口在交接报告中提出所需变更，不直接覆盖这些文件。
4. 共享工作目录中禁止多个窗口同时全书渲染、切换分支、提交或推送。不得使用 git reset/clean 撤销其他窗口工作，不执行 git add .。独立 worktree 的分支提交可按明确分工进行，公共发布仍由整合窗口完成。
5. 每章在 logs/<topic>/handoff.md 记录修改文件、数据来源、环境、执行命令、真实输出、待处理事项及公共配置变更请求。logs 不发布；证据需本地保留，跨 worktree 整合时同时移交相关报告。
6. 图片使用主题专属唯一文件名。章节窗口可按 PicGo 规则上传自己的图，在 figs/<topic>-uploaded-images.md 记录链接；整合窗口汇入 figs/uploaded-images.md，避免同时追加同一个清单。
7. 整合窗口检查代码与输出一致、公式解释完整、数据与文献可追溯，再接入导航并全书渲染；检查链接、编号、图片、页面返回入口后提交本轮明确范围。
